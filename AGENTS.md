# BobVirus 타일 작업 지침

## 적용 범위

이 지침은 타일 스프라이트, Tile·RuleTile 에셋, TileList, Tilemap을 수정할 때 적용한다. 작업 전 docs/tiles/tile-system-reference.md에서 관련 구조를 확인한다.

현재 브랜치의 구현을 조사하되, 이 문서의 디자인 불변식을 단일 씬 사례로 덮어쓰지 않는다. 구현과 불변식이 다르면 차이를 보고하고 불변식을 기준으로 처리한다.

## 핵심 원칙

벽·문 점유와 가구 방향 규칙은 최우선 디자인 불변식이다.

- 맵 지형은 Grid 아래 Tilemap에 TileBase로 배치한다.
- Floor는 바닥, Wall은 벽·문·정적·동적 가구를 담당한다.
- 이웃을 판정하는 타일은 별도 교차 Tilemap 로직이 없다면 같은 Wall Tilemap에 둔다.
- 정적 타일은 m_DefaultGameObject와 모든 m_TilingRules[].m_GameObject를 null로 유지한다.
- 사용자 검수 전에는 Tile Palette의 생성, 삭제, 재배치, 등록 변경을 하지 않는다.
- Funiture, Assets_TIles, outterWall 철자를 임의로 바꾸지 않는다.
- 기존 GUID를 복제하지 않고 새 에셋에는 고유 GUID를 사용한다.
- 벽은 항상 논리적으로 2셀 두께다. 가로 벽은 2개 행, 세로 벽은 2개 열을 채운다.
- 벽·문 스프라이트는 16×32, PPU 16, pivot (0.5, 0.25)을 기준으로 한다.
- 논리적 2셀 두께와 16×32 스프라이트 높이는 별개의 필수 조건이다.
- 문 하나는 동일 Door RuleTile의 인접한 2셀이다. 가로 벽의 문은 세로 2셀, 세로 벽의 문은 가로 2셀로 배치한다.
- 문이 차지하는 두 셀에는 벽 타일을 남기지 않는다.
- 방향이 있는 가구는 항상 상·좌·우·하 네 방향을 제작한다. 한 방향 스프라이트를 회전해 대체하지 않는다.
- 방향성 조립형 가구는 네 방향마다 필요한 모든 조각을 갖추고 방향 Target과 조립 This 조건을 보존한다.
- table처럼 방향 대신 연결 상태를 표현하는 가구는 검증된 This·NotThis 토폴로지를 보존한다.
- RuleTile은 직렬화 순서의 첫 일치 규칙을 사용한다. m_Id로 정렬하지 않고 구체적인 문·Target·대각선 규칙을 먼저 둔다.
- 복잡한 AdvancedRuleTile을 단순 4방향 마스크로 축약하지 않는다.

## 작업 순서

1. 실제 사용 에셋의 경로, GUID, 규칙 순서, TileList 참조를 확인한다.
2. 스프라이트와 .meta의 slice, pivot, PPU, 방향·조각 구성을 확인한다.
3. 씬 좌표와 sprite.bounds로 실제 점유 영역을 확인한다.
4. 기존 토폴로지를 유지하는 최소 변경만 적용한다.
5. 영향받은 에셋만 리임포트한다. Tilemap 또는 씬을 변경한 경우에만 Refresh하고 저장한다.

## 필수 검증

- PPU, 필터, Mipmap, 압축, Wrap, slice, pivot을 기준 에셋과 대조한다.
- 벽 2셀 두께, 문 방향별 2셀, 가구 4방향 구성을 확인한다.
- 규칙 순서와 첫 일치 결과를 확인하고 Rule ID 중복이 없는지 검사한다.
- 직선, 모서리, 분기, 문·가구 접점의 출력 스프라이트를 확인한다.
- 정적 타일의 기본 및 규칙별 GameObject가 모두 null인지 확인한다.
- 벽·문·가구 Bounds와 Collider가 통로를 침범하지 않는지 확인한다.
- C#을 변경했다면 컴파일하고, 씬을 변경했다면 저장 성공을 확인한다.
- 요청하지 않은 Tile Palette 변경이 없는지 확인한다.

## 예외

동적 가구도 Wall Tilemap의 역할에 포함된다. 애니메이션, 런타임 상태, 독립 Collider, 상호작용 때문에 GameObject가 필요하면 정적 타일 원칙의 명시적 예외로 구현하고 이웃 판정과 Collider 동작을 함께 검증한다.
