# BobVirus 타일 작업 지침

## 적용 범위

이 지침은 타일 스프라이트, Tile·RuleTile 에셋, TileList, Tilemap을 수정할 때 적용한다. 작업 전 docs/tiles/tile-system-reference.md에서 관련 구조를 확인한다.

현재 브랜치의 코드, 씬, 에셋이 최종 기준이다. 문서와 다르면 추측해 맞추지 말고 차이를 보고한다.

## 핵심 원칙

- 맵 지형은 Grid 아래 Tilemap에 TileBase로 배치한다.
- Main 씬의 Floor는 바닥, Wall은 벽·문·정적 가구를 담당한다.
- 이웃을 판정하는 타일은 별도 교차 Tilemap 로직이 없다면 같은 Tilemap에 둔다.
- 정적 맵 타일의 m_DefaultGameObject는 null로 유지한다.
- 사용자 검수 전에는 Tile Palette의 생성, 삭제, 재배치, 등록 변경을 하지 않는다.
- 기존 경로와 에셋 이름의 Funiture, Assets_TIles, outterWall 철자를 바꾸지 않는다.
- 기존 GUID를 복제하지 않는다. 새 에셋에는 고유 GUID를 사용한다.
- 크기, 피벗, 점유 셀 수, 연결 토폴로지를 일반화하지 말고 기준 에셋과 씬에서 확인한다.
- 복잡한 AdvancedRuleTile은 단순 4방향 규칙으로 축약하지 않는다.

## 작업 순서

1. 실제 사용 에셋의 경로, GUID, 규칙 수·순서, TileList 참조를 확인한다.
2. 원본 스프라이트와 .meta의 slice, pivot, PPU, 임포트 설정을 확인한다.
3. 씬의 타일 좌표와 스프라이트 Bounds로 실제 점유 영역을 확인한다.
4. 기존 구조를 유지하는 최소 변경만 적용한다.
5. 리임포트, 타일 Refresh, 씬 저장 후 결과를 검증한다.

## 필수 검증

- 수정한 스프라이트의 PPU, 필터, Mipmap, 압축, Wrap 설정을 기준 에셋과 대조한다.
- 대표 좌표에서 직선, 모서리, 분기, 문·가구 접점의 출력 스프라이트를 확인한다.
- 벽, 문, 가구의 실제 점유 영역과 Collider가 통로를 침범하지 않는지 확인한다.
- 수정한 맵 요소 아래에 불필요한 SpriteRenderer가 생기지 않았는지 확인한다.
- C#을 변경했다면 컴파일하고, 씬을 변경했다면 저장 성공을 확인한다.
- 요청하지 않은 Tile Palette 변경이 없는지 확인한다.

## 예외

애니메이션, 런타임 상태, 독립 Collider, 상호작용 컴포넌트가 필요한 요소는 TileBase 원칙의 예외가 될 수 있다. 기존 구현에서 확인되지 않은 예외는 사용자와 합의한 뒤 도입한다.
